#include <stdio.h>
int main () {
    
    
    int a,b;
    // a and b are sides of a rectangle
    printf("enter a number");
    scanf("%d", &a);

    printf("enter a number");
    scanf("%d", &b);

    printf("perimeter is %d \n", 2 * (a + b));
    return 0;

}
